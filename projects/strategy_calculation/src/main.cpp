#include <set>
#include <algorithm>
#include <fstream>
#include <memory>
#include <minimax_controller.hpp>

int main() {

    TMinimaxController minimaxController(2);
    minimaxController.start();
    return 0;
}
